import { render } from '@testing-library/react';
import { AffiliateBannerInSidebar } from './AffiliateBannerInSidebar'

describe('<AffiliateBannerInSidebar />', () => {
  it('should render text', () => {
    const { findByText } = render(<)
  });
});