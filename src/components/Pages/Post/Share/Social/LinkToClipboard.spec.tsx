import { SharePostLinkToClipboard } from './LinkToClipboard'
import { render } from '@testing-library/react'

describe('SharePostLinkToClipboard', () => {
  const postUrl = 'https://dmitripavlutin.com/some-post/'

  describe('when the link is clicked', () => {
    it('should prevent the default link action', () => {

    })
  })
})